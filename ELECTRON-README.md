# تبدیل داشبورد به اپلیکیشن ویندوز (Electron)

پروژه الان علاوه بر وب‌اپ، آماده‌ست که به یک فایل نصبی ویندوز (`.exe`) تبدیل بشه.
سرور محلی (`server/index.js`) بدون هیچ تغییری همون‌طور که بود باقی مونده — Electron
فقط اون رو به‌عنوان یک پروسه‌ی فرزند اجرا می‌کنه و صفحه‌ی ساخته‌شده‌ی React رو نمایش
می‌ده.

## چیزهایی که اضافه شده

- `electron/main.cjs` — پروسه‌ی اصلی Electron: سرور Express رو اجرا می‌کنه و پنجره
  رو باز می‌کنه
- `electron/preload.cjs` — فعلاً خالیه (برای آینده نگه داشته شده)
- اسکریپت‌های جدید در `package.json`:
  - `npm run electron:dev` — اجرای برنامه در حالت توسعه (هم Vite هم Electron با هم)
  - `npm run electron:build` — ساخت فایل نصبی نهایی ویندوز (`.exe`)
  - `npm run electron:build:dir` — ساخت نسخه‌ی unpacked بدون installer (برای تست سریع)

## مراحل اجرا روی سیستم خودت

### ۱) نصب وابستگی‌ها

```bash
npm install
cd server
npm install
cd ..
```

### ۲) تست در حالت توسعه (اختیاری، برای دیدن این‌که همه‌چیز درست کار می‌کنه)

```bash
npm run electron:dev
```

این هم Vite رو بالا می‌آره، هم پنجره‌ی Electron رو باز می‌کنه و به Vite وصل می‌شه.
سرور محلی (پورت ۴۳۱۰) هم به‌صورت خودکار در پس‌زمینه اجرا می‌شه.

### ۳) ساخت فایل نصبی نهایی

```bash
npm run electron:build
```

بعد از اتمام، فایل نصبی (`Dashboard Setup x.x.x.exe`) داخل پوشه‌ی `dist/` قرار
می‌گیره. این فایل رو اجرا کن تا برنامه مثل هر نرم‌افزار ویندوزی دیگه نصب بشه،
با آیکون روی دسکتاپ و منوی استارت.

> ⚠️ نکته: چون `electron-builder` باید فایل‌های باینری خود Electron برای ویندوز رو
> دانلود کنه، این مرحله باید حتماً روی سیستم خودت (یا هر ماشینی با دسترسی اینترنت
> عادی) اجرا بشه، نه داخل یک محیط sandbox محدود.

## نکات مهم درباره‌ی نحوه‌ی کارکرد

- **سرور محلی**: همون `server/index.js` قبلی، بدون تغییر. Electron به‌محض باز شدن،
  اون رو با `node` اجرا می‌کنه و وقتی برنامه بسته بشه، خودش هم بسته می‌شه.
- **yt-dlp**: منطق پیدا کردن yt-dlp (از PATH سیستم یا دانلود خودکار در
  `~/.dashboard-downloader-bin`) دقیقاً مثل قبل کار می‌کنه، چون کد سرور تغییری
  نکرده و از `os.homedir()` واقعی سیستم استفاده می‌کنه.
- **ذخیره‌سازی محلی**: فایل‌های JSON در `server/storage` (داخل پوشه‌ی نصب برنامه در
  حالت پکیج‌شده) ذخیره می‌شن، دقیقاً مثل قبل.
- **Google Drive**: فعلاً در کد غیرفعال (کامنت) هست؛ اگه بعداً فعالش کردی، لازمه
  Authorized JavaScript origin توی Google Cloud Console رو برای پروتکل/پورتی که
  Electron لود می‌کنه (`file://` یا `http://localhost`) تنظیم کنی.
- **آیکون برنامه**: فعلاً آیکون پیش‌فرض Electron استفاده می‌شه. اگه بخوای آیکون
  اختصاصی بذاری، یک فایل `icon.ico` (حداقل ۲۵۶×۲۵۶) داخل `public/` بذار و در
  `package.json` زیر `build.win` این خط رو اضافه کن:
  ```json
  "icon": "public/icon.ico"
  ```

## این پروژه با Electron ساخته شده، نه Tauri — چرا؟

چون بخش دانلودر و ذخیره‌سازی محلی پروژه یک سرور Node/Express واقعی داره که از
`spawn`/`exec` برای اجرای yt-dlp استفاده می‌کنه. Electron همون Node.js رو زیرش
داره و اجرای این سرور به‌عنوان child process کاملاً طبیعی و بدون بازنویسی کد
انجام می‌شه. Tauri از Rust استفاده می‌کنه و برای این کار نیاز به بازنویسی کامل
بک‌اند به Rust بود.
